import styled from '@emotion/styled';

export const NotificationText = styled.p`
  font-family: inherit;
  font-style: normal;
  font-weight: 500;
  font-size: 25px;
  line-height: 1.15;
  color: tomato;
`;
